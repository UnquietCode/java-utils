apply plugin: 'java'

sourceCompatibility = 1.8
version = '0.1'
group = 'com.unquietcode.utils'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

task wrapper(type: Wrapper) {
	gradleVersion = '4.8'
}